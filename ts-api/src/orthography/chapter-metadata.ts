export type ChapterMetadata = {
  chapterNumber: number;
  verseCount: number;
  phonetic: string;
  translation: string;
  city: string;
};

export type ChapterInfo = {
  chapterNumber: number;
  phonetic: string;
  translation: string;
  city: string;
};
