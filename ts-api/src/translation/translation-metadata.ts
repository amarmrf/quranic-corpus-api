export type TranslationMetadata = {
  key: string;
  name: string;
};

export type TranslationInfo = {
  name: string;
};

export type TranslationResponse = {
  name: string;
  translation: string;
};
